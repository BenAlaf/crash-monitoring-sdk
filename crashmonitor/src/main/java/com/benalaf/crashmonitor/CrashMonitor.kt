package com.benalaf.crashmonitor

/**
 * Public entry point of the CrashMonitor SDK.
 *
 * Phase 0 placeholder — the real implementation (crash handler, disk queue,
 * uploader) lands in Phase 2. See IMPLEMENTATION_PLAN.md §5.
 */
object CrashMonitor {

    const val SDK_VERSION: String = "0.1.0"
}
